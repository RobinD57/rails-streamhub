<div class="outer-box-form-home">
  <div class="login-title">
    <h6 class="spaced">Get started in 1 click: </h6>
  </div>
  <%= link_to user_twitch_omniauth_authorize_path, class: "stream-button" do %>
  <div class='signin-button shadowed box-shadow extra-spaced'><i class="fab fa-twitch shadowed"> </i><span class="shadowed"> Sign in with Twitch</span></div>
  <% end %>
  <%= link_to user_mixer_omniauth_authorize_path, class: "stream-button" do %>
  <div class='signin-button shadowed box-shadow spaced'><i class="fab fa-mixer shadowed"> </i><span class="shadowed"> Sign in with Mixer </span></div>
  <% end %>
  <%= link_to user_twitch_omniauth_authorize_path, class: "stream-button" do %>
  <div class='signin-button shadowed box-shadow spaced'><i class="fas fa-user-ninja shadowed"> </i><span class="shadowed"> Sign in with Dlive</span></div>
  <% end %>
  <%= link_to user_google_oauth2_omniauth_authorize_path, class: "stream-button" do %>
  <div class='signin-button shadowed box-shadow spaced'><i class="fab fa-youtube shadowed"> </i><span class="shadowed"> Sign in with Youtube </span></div>
  <% end %>

  <div class="d-flex justify-content-center">
    <hr class="divider" />
  </div>

 <!--  <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="form-inputs">
      <%= f.input :email,
      required: false,
      autofocus: true,
      label: false,
      input_html: { class: "login-input", placeholder: "email", autocomplete: "email" } %>
      <%= f.input :password,
      required: false,
      label: false,
      input_html: { class: "login-input", placeholder: "password", autocomplete: "current-password" } %>
      <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
    </div>

  <div class="form-actions">
    <%= f.button :submit, "Log in", { class: "submit-login-button" }%>
  </div>
  <% end %> -->

  <div class="text-center">
    <%= link_to "Sign up with email", new_registration_path(resource_name), {class: "signup-devise-link shadowed white mt-2 stroke"} %>
    <br>
    <br>
    <!-- <%= link_to "Forgot your password?", new_password_path(resource_name), {class: "forgotpass-devise-link shadowed white stroke"} %> -->
  </div>

</div>


